// console.log("connected!");
// let str = "bitch!";

let arr = ["fuck", "shit", "bitch", "hoe"];

function yell(str) {
  console.log(str.toUpperCase());
}

function myForEach(arr, func) {
  for (let i = 0; i < arr.length; i++) {
    console.log(arr[i], i);
    // func(str);
  }
}
